<SNT> cityServed </SNT> 
<SNT> location </SNT> 
<SNT> runwayName </SNT> 
<SNT> ICAO_Location_Identifier </SNT> 
<SNT> cityServed </SNT> 
<SNT> cityServed </SNT> 
<SNT> runwayLength </SNT> 
<SNT> runwayName </SNT> 
<SNT> runwayLength </SNT> 
<SNT> 1st_runway_Number </SNT> 
<SNT> 5th_runway_SurfaceType </SNT> 
<SNT> runwayName </SNT> 
<SNT> elevationAboveTheSeaLevel_(in_metres) </SNT> 
<SNT> owner </SNT> 
<SNT> runwayLength </SNT> 
<SNT> 1st_runway_SurfaceType </SNT> 
<SNT> elevationAboveTheSeaLevel_(in_metres) </SNT> 
<SNT> operatingOrganisation </SNT> 
<SNT> runwayLength </SNT> 
<SNT> runwayLength </SNT> 
<SNT> ICAO_Location_Identifier </SNT> 
<SNT> runwayName </SNT> 
<SNT> owner </SNT> 
<SNT> runwayName </SNT> 
<SNT> language </SNT> 
<SNT> isPartOf </SNT> 
<SNT> demonym </SNT> 
<SNT> country </SNT> 
<SNT> country </SNT> 
<SNT> isPartOf </SNT> 
<SNT> language </SNT> 
<SNT> country </SNT> 
<SNT> isPartOf </SNT> 
<SNT> leaderName </SNT> 
<SNT> regionServed </SNT> 
<SNT> largestCity </SNT> 
<SNT> battles </SNT> 
<SNT> transportAircraft </SNT> 
<SNT> <SNT> </SNT> 
<SNT> <SNT> </SNT> 
<SNT> <SNT> </SNT> 
<SNT> genre </SNT> 
<SNT> genre </SNT> 
<SNT> <SNT> </SNT> 
<SNT> birthDate </SNT> 
<SNT> birthPlace </SNT> 
<SNT> genre </SNT> 
<SNT> genre </SNT> 
<SNT> birthPlace </SNT> 
<SNT> genre </SNT> 
<SNT> <SNT> </SNT> 
<SNT> <SNT> </SNT> 
<SNT> birthPlace </SNT> 
<SNT> genre </SNT> 
<SNT> genre </SNT> 
<SNT> genre </SNT> 
<SNT> <SNT> </SNT> 
<SNT> birthPlace </SNT> 
<SNT> deathPlace </SNT> 
<SNT> <SNT> </SNT> 
<SNT> </SNT> <SNT> </SNT> 
<SNT> genre </SNT> 
<SNT> <SNT> </SNT> 
<SNT> birthDate </SNT> 
<SNT> genre </SNT> 
<SNT> location </SNT> 
<SNT> <SNT> </SNT> 
<SNT> genre </SNT> 
<SNT> <SNT> </SNT> 
<SNT> genre </SNT> 
<SNT> <SNT> </SNT> 
<SNT> <SNT> </SNT> 
<SNT> birthPlace </SNT> 
<SNT> <SNT> </SNT> 
<SNT> <SNT> </SNT> 
<SNT> isPartOf </SNT> 
<SNT> <SNT> </SNT> 
<SNT> <SNT> </SNT> 
<SNT> <SNT> </SNT> 
<SNT> <SNT> </SNT> 
<SNT> isPartOf </SNT> 
<SNT> location </SNT> 
<SNT> <SNT> </SNT> 
<SNT> <SNT> </SNT> 
<SNT> <SNT> </SNT> 
<SNT> demonym </SNT> 
<SNT> nationality </SNT> 
<SNT> backup_pilot </SNT> 
<SNT> almaMater </SNT> 
<SNT> was_a_crew_member_of </SNT> 
<SNT> was_selected_by_NASA </SNT> 
<SNT> gemstone </SNT> 
<SNT> deathDate </SNT> 
<SNT> deathPlace </SNT> 
<SNT> status </SNT> 
<SNT> chairman </SNT> 
<SNT> birthPlace </SNT> 
<SNT> club </SNT> <SNT> </SNT> 
<SNT> club </SNT> <SNT> </SNT> 
<SNT> club </SNT> <SNT> </SNT> 
<SNT> birthPlace </SNT> 
<SNT> birthPlace </SNT> 
<SNT> birthPlace </SNT> 
<SNT> club </SNT> <SNT> </SNT> 
<SNT> club </SNT> <SNT> </SNT> 
<SNT> club </SNT> <SNT> </SNT> 
<SNT> birthPlace </SNT> 
<SNT> <SNT> </SNT> 
<SNT> club </SNT> <SNT> </SNT> 
<SNT> league </SNT> 
<SNT> manager </SNT> 
<SNT> <SNT> </SNT> 
<SNT> birthPlace </SNT> 
<SNT> club </SNT> <SNT> </SNT> 
<SNT> club </SNT> <SNT> </SNT> 
<SNT> club </SNT> <SNT> </SNT> 
<SNT> birthDate </SNT> 
<SNT> club </SNT> <SNT> </SNT> 
<SNT> height </SNT> 
<SNT> league </SNT> 
<SNT> birthDate </SNT> 
<SNT> club </SNT> <SNT> </SNT> 
<SNT> birthPlace </SNT> 
<SNT> club </SNT> <SNT> </SNT> 
<SNT> club </SNT> <SNT> </SNT> 
<SNT> club </SNT> 
<SNT> youthclub </SNT> 
<SNT> birthPlace </SNT> 
<SNT> league </SNT> 
<SNT> city </SNT> 
<SNT> <SNT> </SNT> 
<SNT> country </SNT> 
<SNT> language </SNT> 
<SNT> language </SNT> 
<SNT> city </SNT> 
<SNT> manager </SNT> 
<SNT> manager </SNT> 
<SNT> chairman </SNT> 
<SNT> language </SNT> 
<SNT> <SNT> </SNT> 
<SNT> <SNT> </SNT> 
<SNT> ground </SNT> 
<SNT> owner </SNT> 
<SNT> city </SNT> 
<SNT> city </SNT> 
<SNT> location </SNT> 
<SNT> architecturalStyle </SNT> 
<SNT> location </SNT> 
<SNT> architect </SNT> 
<SNT> buildingStartDate </SNT> 
<SNT> numberOfRooms </SNT> 
<SNT> architect </SNT> 
<SNT> location </SNT> 
<SNT> country </SNT> 
<SNT> owner </SNT> 
<SNT> address </SNT> 
<SNT> ReferenceNumber_in_the_National_Register_of_Historic_Places </SNT> 
<SNT> architect </SNT> 
<SNT> architecture </SNT> 
<SNT> yearOfConstruction </SNT> 
<SNT> location </SNT> 
<SNT> isPartOf </SNT> 
<SNT> leaderName </SNT> 
<SNT> isPartOf </SNT> 
<SNT> leaderName </SNT> 
<SNT> leaderName </SNT> 
<SNT> demonym </SNT> 
<SNT> significantBuilding </SNT> 
<SNT> location </SNT> 
<SNT> isPartOf </SNT> 
<SNT> isPartOf </SNT> 
<SNT> isPartOf </SNT> 
<SNT> ethnicGroup </SNT> 
<SNT> leaderName </SNT> 
<SNT> <SNT> </SNT> 
<SNT> <SNT> </SNT> 
<SNT> <SNT> </SNT> 
<SNT> <SNT> </SNT> 
<SNT> <SNT> </SNT> 
<SNT> <SNT> </SNT> 
<SNT> <SNT> </SNT> 
<SNT> formerName </SNT> 
<SNT> <SNT> </SNT> 
<SNT> <SNT> </SNT> 
<SNT> <SNT> </SNT> 
<SNT> <SNT> </SNT> 
<SNT> <SNT> </SNT> 
<SNT> <SNT> </SNT> 
<SNT> <SNT> </SNT> 
<SNT> <SNT> </SNT> 
<SNT> <SNT> </SNT> 
<SNT> formerName </SNT> 
<SNT> <SNT> </SNT> 
<SNT> <SNT> </SNT> 
<SNT> <SNT> </SNT> 
<SNT> <SNT> </SNT> 
<SNT> <SNT> </SNT> 
<SNT> <SNT> </SNT> 
<SNT> <SNT> </SNT> 
<SNT> <SNT> </SNT> 
<SNT> <SNT> </SNT> 
<SNT> <SNT> </SNT> 
<SNT> almaMater </SNT> 
<SNT> birthPlace </SNT> 
<SNT> <SNT> </SNT> 
<SNT> isPartOf </SNT> 
<SNT> leaderTitle </SNT> 
<SNT> leaderTitle </SNT> 
<SNT> leaderTitle </SNT> 
<SNT> <SNT> </SNT> 
<SNT> isPartOf </SNT> 
<SNT> isPartOf </SNT> 
<SNT> <SNT> </SNT> 
<SNT> <SNT> </SNT> 
<SNT> isPartOf </SNT> 
<SNT> <SNT> </SNT> 
<SNT> leader </SNT> 
<SNT> </SNT> <SNT> </SNT> 
<SNT> isPartOf </SNT> 
<SNT> <SNT> </SNT> 
<SNT> isPartOf </SNT> 
<SNT> <SNT> </SNT> 
<SNT> isPartOf </SNT> 
<SNT> language </SNT> 
<SNT> largestCity </SNT> 
<SNT> country </SNT> 
<SNT> countySeat </SNT> 
<SNT> country </SNT> 
<SNT> largestCity </SNT> 
<SNT> country </SNT> 
<SNT> largestCity </SNT> 
<SNT> largestCity </SNT> 
<SNT> leader </SNT> 
<SNT> location </SNT> 
<SNT> creator </SNT> 
<SNT> broadcastedBy </SNT> 
<SNT> fullName </SNT> 
<SNT> creator </SNT> 
<SNT> fullName </SNT> 
<SNT> birthPlace </SNT> 
<SNT> alternativeName </SNT> 
<SNT> award </SNT> 
<SNT> nationality </SNT> 
<SNT> award </SNT> 
<SNT> nationality </SNT> 
<SNT> birthPlace </SNT> 
<SNT> ingredient </SNT> 
<SNT> ingredient </SNT> 
<SNT> ingredient </SNT> 
<SNT> region </SNT> <SNT> </SNT> 
<SNT> region </SNT> 
<SNT> region </SNT> 
<SNT> ingredient </SNT> 
<SNT> region </SNT> 
<SNT> served </SNT> 
<SNT> country </SNT> 
<SNT> ingredient </SNT> 
<SNT> region </SNT> <SNT> </SNT> 
<SNT> servingSize </SNT> 
<SNT> dishVariation </SNT> 
<SNT> mainIngredients </SNT> 
<SNT> ingredient </SNT> 
<SNT> region </SNT> 
<SNT> alternativeName </SNT> 
<SNT> ingredient </SNT> 
<SNT> dishVariation </SNT> 
<SNT> region </SNT> 
<SNT> class </SNT> 
<SNT> currency </SNT> 
<SNT> leaderName </SNT> 
<SNT> language </SNT> 
<SNT> leaderName </SNT> 
<SNT> ethnicGroup </SNT> 
<SNT> ethnicGroup </SNT> 
<SNT> leaderName </SNT> 
<SNT> language </SNT> 
<SNT> country </SNT> 
<SNT> ingredient </SNT> 
<SNT> ethnicGroup </SNT> 
<SNT> leaderName </SNT> 
<SNT> alternativeName </SNT> 
<SNT> <SNT> </SNT> 
<SNT> <SNT> </SNT> 
<SNT> location </SNT> 
<SNT> operator </SNT> 
<SNT> <SNT> </SNT> 
<SNT> completionDate </SNT> 
<SNT> <SNT> </SNT> 
<SNT> operator </SNT> 
<SNT> <SNT> </SNT> 
<SNT> alternativeName </SNT> 
<SNT> status </SNT> 
<SNT> <SNT> </SNT> 
<SNT> <SNT> </SNT> 
<SNT> <SNT> </SNT> 
<SNT> <SNT> </SNT> 
<SNT> <SNT> </SNT> 
<SNT> <SNT> </SNT> 
<SNT> <SNT> </SNT> 
<SNT> <SNT> </SNT> 
<SNT> <SNT> </SNT> 
<SNT> <SNT> </SNT> 
<SNT> status </SNT> 
<SNT> <SNT> </SNT> 
<SNT> <SNT> </SNT> 
<SNT> <SNT> </SNT> 
<SNT> <SNT> </SNT> 
<SNT> <SNT> </SNT> 
<SNT> <SNT> </SNT> 
<SNT> leaderName </SNT> 
<SNT> <SNT> </SNT> 
<SNT> <SNT> </SNT> 
<SNT> <SNT> </SNT> 
<SNT> <SNT> </SNT> 
<SNT> <SNT> </SNT> 
<SNT> <SNT> </SNT> 
<SNT> <SNT> </SNT> 
<SNT> foundedBy </SNT> 
<SNT> <SNT> </SNT> 
<SNT> <SNT> </SNT> 
<SNT> location </SNT> 
<SNT> location </SNT> 
<SNT> <SNT> </SNT> 
<SNT> <SNT> </SNT> 
<SNT> demonym </SNT> 
<SNT> leaderName </SNT> 
<SNT> leaderName </SNT> 
<SNT> foundedBy </SNT> 
<SNT> headquarter </SNT> 
<SNT> <SNT> </SNT> 
<SNT> anthem </SNT> 
<SNT> ethnicGroup </SNT> 
<SNT> established </SNT> 
<SNT> has_to_its_southeast </SNT> 
<SNT> leader </SNT> 
<SNT> leaderTitle </SNT> 
<SNT> nativeName </SNT> 
<SNT> award </SNT> 
<SNT> battles </SNT> 
<SNT> birthPlace </SNT> 
<SNT> <SNT> </SNT> 
<SNT> <SNT> </SNT> 
<SNT> <SNT> </SNT> 
<SNT> <SNT> </SNT> 
<SNT> birthPlace </SNT> 
<SNT> birthPlace </SNT> 
<SNT> <SNT> </SNT> 
<SNT> deathPlace </SNT> 
<SNT> birthPlace </SNT> 
<SNT> residence </SNT> 
<SNT> almaMater </SNT> 
<SNT> birthPlace </SNT> 
<SNT> <SNT> </SNT> 
<SNT> <SNT> </SNT> 
<SNT> birthPlace </SNT> 
<SNT> birthPlace </SNT> 
<SNT> <SNT> </SNT> 
<SNT> award </SNT> 
<SNT> birthPlace </SNT> 
<SNT> <SNT> </SNT> 
<SNT> <SNT> </SNT> 
<SNT> <SNT> </SNT> 
<SNT> <SNT> </SNT> 
<SNT> <SNT> </SNT> 
<SNT> nationality </SNT> 
<SNT> <SNT> </SNT> 
<SNT> deathPlace </SNT> 
<SNT> <SNT> </SNT> 
<SNT> battles </SNT> 
<SNT> birthPlace </SNT> 
<SNT> <SNT> </SNT> 
<SNT> <SNT> </SNT> 
<SNT> <SNT> </SNT> 
<SNT> <SNT> </SNT> 
<SNT> <SNT> </SNT> 
<SNT> <SNT> </SNT> 
<SNT> commander </SNT> 
<SNT> language </SNT> 
<SNT> commander </SNT> 
<SNT> <SNT> </SNT> 
<SNT> leaderName </SNT> 
<SNT> commander </SNT> 
<SNT> <SNT> </SNT> 
<SNT> almaMater </SNT> 
<SNT> <SNT> </SNT> 
<SNT> <SNT> </SNT> 
<SNT> leaderName </SNT> 
<SNT> battles </SNT> 
<SNT> birthPlace </SNT> 
<SNT> manager </SNT> 
<SNT> league </SNT> 
<SNT> fullname </SNT> 
<SNT> ground </SNT> 
<SNT> ground </SNT> 
<SNT> ground </SNT> 
<SNT> numberOfMembers </SNT> 
<SNT> ground </SNT> 
<SNT> season </SNT> 
<SNT> location </SNT> 
<SNT> league </SNT> 
<SNT> season </SNT> 
<SNT> country </SNT> 
<SNT> location </SNT> 
<SNT> fullname </SNT> 
<SNT> leader </SNT> 
<SNT> club </SNT> <SNT> </SNT> 
<SNT> club </SNT> 
<SNT> club </SNT> 
<SNT> region </SNT> <SNT> </SNT> 
<SNT> leader </SNT> 
<SNT> club </SNT> <SNT> </SNT> 
<SNT> club </SNT> 
<SNT> club </SNT> <SNT> </SNT> 
<SNT> club </SNT> 
<SNT> club </SNT> 
<SNT> club </SNT> <SNT> </SNT> 
<SNT> club </SNT> 
<SNT> club </SNT> 
<SNT> tenant </SNT> 
<SNT> country </SNT> 
<SNT> state </SNT> 
<SNT> president </SNT> 
<SNT> has_to_its_northwest </SNT> 
<SNT> neighboringMunicipality </SNT> 
<SNT> capital </SNT> 
<SNT> dean </SNT> 
<SNT> numberOfStudents </SNT> 
<SNT> ISBN_number </SNT> 
<SNT> author </SNT> 
<SNT> mediaType </SNT> 
<SNT> country </SNT> 
<SNT> OCLC_number </SNT> 
<SNT> mediaType </SNT> 
<SNT> language </SNT> 
<SNT> author </SNT> 
<SNT> ISSN_number </SNT> 
<SNT> author </SNT> 
<SNT> mediaType </SNT> 
<SNT> ISSN_number </SNT> 
<SNT> LCCN_number </SNT> 
<SNT> academicDiscipline </SNT> 
<SNT> language </SNT> 
<SNT> country </SNT> 
<SNT> language </SNT> 
<SNT> academicDiscipline </SNT> 
<SNT> language </SNT> 
<SNT> birthPlace </SNT> 
<SNT> parentCompany </SNT> 
<SNT> notableWork </SNT> 
<SNT> country </SNT> 
<SNT> publisher </SNT> 
<SNT> ethnicGroup </SNT> 
<SNT> language </SNT> 
<SNT> precededBy </SNT> 
<SNT> operatingOrganisation runwayLength </SNT> 
<SNT> location elevationAboveTheSeaLevel_(in_metres) </SNT> 
<SNT> location </SNT> <SNT> runwayLength </SNT> 
<SNT> location isPartOf </SNT> 
<SNT> location runwayLength </SNT> 
<SNT> location language </SNT> 
<SNT> battles operatingOrganisation </SNT> 
<SNT> 1st_runway_SurfaceType class </SNT> 
<SNT> 1st_runway_SurfaceType runwayLength </SNT> 
<SNT> location leaderName </SNT> 
<SNT> location runwayLength </SNT> 
<SNT> elevationAboveTheSeaLevel_(in_metres) </SNT> 
<SNT> elevationAboveTheSeaLevel_(in_metres) 1st_runway_SurfaceType </SNT> 
<SNT> cityServed runwayLength </SNT> 
<SNT> cityServed runwayLength </SNT> 
<SNT> location largestCity </SNT> 
<SNT> cityServed runwayLength </SNT> 
<SNT> runwayLength 3rd_runway_SurfaceType </SNT> 
<SNT> elevationAboveTheSeaLevel_(in_metres) 1st_runway_LengthFeet </SNT> 
<SNT> operatingOrganisation headquarter </SNT> 
<SNT> location country </SNT> 
<SNT> operatingOrganisation runwayName </SNT> 
<SNT> operatingOrganisation runwayLength </SNT> 
<SNT> operatingOrganisation location </SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> birthPlace birthDate </SNT> 
<SNT> birthPlace birthPlace </SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> birthPlace </SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> birthPlace </SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> birthPlace deathPlace </SNT> 
<SNT> birthPlace deathPlace </SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> location </SNT> 
<SNT> birthDate status </SNT> 
<SNT> status occupation </SNT> 
<SNT> birthPlace birthDate </SNT> 
<SNT> birthPlace isPartOf </SNT> 
<SNT> birthPlace was_a_crew_member_of </SNT> 
<SNT> status almaMater </SNT> 
<SNT> birthPlace club </SNT> 
<SNT> manager club </SNT> 
<SNT> club </SNT> <SNT> </SNT> 
<SNT> club club </SNT> 
<SNT> club </SNT> <SNT> </SNT> 
<SNT> club </SNT> <SNT> </SNT> 
<SNT> manager club </SNT> 
<SNT> club </SNT> <SNT> </SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> birthDate </SNT> 
<SNT> club </SNT> <SNT> </SNT> 
<SNT> club </SNT> <SNT> </SNT> 
<SNT> club ground </SNT> 
<SNT> manager club </SNT> 
<SNT> club </SNT> <SNT> </SNT> 
<SNT> club ground </SNT> 
<SNT> birthDate club </SNT> 
<SNT> birthPlace club </SNT> 
<SNT> manager club </SNT> 
<SNT> club </SNT> <SNT> </SNT> 
<SNT> club league </SNT> 
<SNT> club </SNT> <SNT> </SNT> 
<SNT> birthPlace </SNT> 
<SNT> birthDate club </SNT> 
<SNT> club league </SNT> 
<SNT> birthPlace leaderName </SNT> 
<SNT> birthPlace anthem </SNT> 
<SNT> birthPlace language </SNT> 
<SNT> manager club </SNT> 
<SNT> club ground </SNT> 
<SNT> club league </SNT> 
<SNT> club ground </SNT> 
<SNT> floorCount completionDate </SNT> 
<SNT> floorCount location </SNT> 
<SNT> floorCount floorArea </SNT> 
<SNT> location floorCount </SNT> 
<SNT> country architect </SNT> 
<SNT> architect location </SNT> 
<SNT> location architect </SNT> 
<SNT> tenant location </SNT> 
<SNT> architect completionDate </SNT> 
<SNT> location location </SNT> 
<SNT> location country </SNT> 
<SNT> architecturalStyle location </SNT> 
<SNT> architecturalStyle location </SNT> 
<SNT> country ethnicGroup </SNT> 
<SNT> floorCount address </SNT> 
<SNT> address location </SNT> 
<SNT> state leaderName </SNT> 
<SNT> location country </SNT> 
<SNT> location isPartOf </SNT> 
<SNT> location isPartOf </SNT> 
<SNT> location isPartOf </SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> birthPlace </SNT> 
<SNT> nationality </SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> nationality </SNT> 
<SNT> birthPlace </SNT> 
<SNT> formerName </SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> formerName </SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> country ethnicGroup </SNT> 
<SNT> capital isPartOf </SNT> 
<SNT> leaderTitle country </SNT> 
<SNT> isPartOf country </SNT> 
<SNT> leaderTitle isPartOf </SNT> 
<SNT> isPartOf country </SNT> 
<SNT> isPartOf country </SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> isPartOf country </SNT> 
<SNT> isPartOf country </SNT> 
<SNT> isPartOf country </SNT> 
<SNT> country capital </SNT> 
<SNT> isPartOf country </SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> isPartOf country </SNT> 
<SNT> isPartOf capital </SNT> 
<SNT> country capital </SNT> 
<SNT> country ethnicGroup </SNT> 
<SNT> ethnicGroup isPartOf </SNT> 
<SNT> country ethnicGroup </SNT> 
<SNT> country language </SNT> 
<SNT> leaderTitle </SNT> <SNT> country </SNT> 
<SNT> creator nationality </SNT> 
<SNT> starring broadcastedBy </SNT> 
<SNT> starring broadcastedBy </SNT> 
<SNT> birthPlace starring </SNT> 
<SNT> firstAppearanceInFilm starring </SNT> 
<SNT> firstAppearanceInFilm starring </SNT> 
<SNT> creator series </SNT> 
<SNT> fullName creator </SNT> 
<SNT> creator creator </SNT> 
<SNT> fullName creator </SNT> 
<SNT> ingredient country </SNT> 
<SNT> country leaderName </SNT> 
<SNT> country language </SNT> 
<SNT> leaderName country </SNT> 
<SNT> country ingredient </SNT> 
<SNT> region country </SNT> 
<SNT> mainIngredients country </SNT> 
<SNT> region country </SNT> 
<SNT> dishVariation dishVariation </SNT> 
<SNT> mainIngredients ingredient </SNT> 
<SNT> ingredient alternativeName </SNT> 
<SNT> ingredient dishVariation </SNT> 
<SNT> ingredient country </SNT> 
<SNT> country leaderName </SNT> 
<SNT> ingredient country </SNT> 
<SNT> dishVariation ingredient </SNT> 
<SNT> ingredient country </SNT> 
<SNT> family ingredient </SNT> 
<SNT> ingredient country </SNT> 
<SNT> country leaderName </SNT> 
<SNT> ingredient country </SNT> 
<SNT> dishVariation ingredient </SNT> 
<SNT> country ingredient </SNT> 
<SNT> country leaderName </SNT> 
<SNT> country </SNT> <SNT> mainIngredients </SNT> 
<SNT> country demonym </SNT> 
<SNT> country leaderName </SNT> 
<SNT> country ethnicGroup </SNT> 
<SNT> country mainIngredients </SNT> 
<SNT> mainIngredients ingredient </SNT> 
<SNT> country leaderName </SNT> 
<SNT> ingredient region </SNT> 
<SNT> course dishVariation </SNT> 
<SNT> country leaderName </SNT> 
<SNT> dishVariation ingredient </SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> location operator </SNT> 
<SNT> location operator </SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> designer </SNT> 
<SNT> country </SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> locationCity </SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> country leaderName </SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> parentCompany </SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> division </SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> keyPerson </SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> leaderName </SNT> 
<SNT> parentCompany </SNT> 
<SNT> owner </SNT> 
<SNT> leaderName </SNT> 
<SNT> established category </SNT> 
<SNT> district nearestCity </SNT> 
<SNT> dedicatedTo location </SNT> 
<SNT> nativeName dedicatedTo </SNT> 
<SNT> award battles </SNT> 
<SNT> award battles </SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> birthPlace </SNT> 
<SNT> birthPlace almaMater </SNT> 
<SNT> birthPlace </SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> battles commander </SNT> 
<SNT> battles </SNT> 
<SNT> birthPlace almaMater </SNT> 
<SNT> birthPlace residence </SNT> 
<SNT> award battles </SNT> 
<SNT> birthPlace </SNT> 
<SNT> birthPlace birthPlace </SNT> 
<SNT> birthPlace </SNT> 
<SNT> birthPlace </SNT> 
<SNT> award </SNT> 
<SNT> battles </SNT> 
<SNT> battles commander </SNT> 
<SNT> birthPlace battles </SNT> 
<SNT> battles commander </SNT> 
<SNT> battles commander </SNT> 
<SNT> birthDate </SNT> 
<SNT> birthDate </SNT> 
<SNT> birthPlace deathPlace </SNT> 
<SNT> deathPlace </SNT> 
<SNT> birthPlace </SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> birthPlace birthPlace </SNT> 
<SNT> birthPlace birthPlace </SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> almaMater </SNT> 
<SNT> birthPlace </SNT> 
<SNT> battles </SNT> 
<SNT> battles commander </SNT> 
<SNT> birthPlace </SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> birthDate </SNT> 
<SNT> country currency </SNT> 
<SNT> country leaderName </SNT> 
<SNT> deathPlace country </SNT> 
<SNT> season numberOfMembers </SNT> 
<SNT> ground numberOfMembers </SNT> 
<SNT> placeOfBirth manager </SNT> 
<SNT> manager club </SNT> 
<SNT> numberOfMembers season </SNT> 
<SNT> manager club </SNT> 
<SNT> manager club </SNT> 
<SNT> ground numberOfMembers </SNT> 
<SNT> ground numberOfMembers </SNT> 
<SNT> ground league </SNT> 
<SNT> manager club </SNT> 
<SNT> manager club </SNT> 
<SNT> fullname numberOfMembers </SNT> 
<SNT> ground numberOfMembers </SNT> 
<SNT> league ground </SNT> 
<SNT> ground tenant </SNT> 
<SNT> location ground </SNT> 
<SNT> numberOfMembers season </SNT> 
<SNT> ground region </SNT> 
<SNT> ground leader </SNT> 
<SNT> club manager </SNT> 
<SNT> nickname state </SNT> 
<SNT> affiliation city </SNT> 
<SNT> city president </SNT> 
<SNT> was_given_the_'Technical_Campus'_status_by location </SNT> 
<SNT> state leaderName </SNT> 
<SNT> author precededBy </SNT> 
<SNT> author mediaType </SNT> 
<SNT> abbreviation EISSN_number </SNT> 
<SNT> author mediaType </SNT> 
<SNT> numberOfPages ISBN_number </SNT> 
<SNT> followedBy author </SNT> 
<SNT> OCLC_number mediaType </SNT> 
<SNT> numberOfPages ISBN_number </SNT> 
<SNT> parentCompany publisher </SNT> 
<SNT> author mediaType </SNT> 
<SNT> OCLC_number ISBN_number </SNT> 
<SNT> OCLC_number mediaType </SNT> 
<SNT> language spokenIn </SNT> 
<SNT> ISBN_number mediaType </SNT> 
<SNT> founder publisher </SNT> 
<SNT> ISBN_number </SNT> 
<SNT> OCLC_number ISBN_number </SNT> 
<SNT> author followedBy </SNT> 
<SNT> abbreviation academicDiscipline </SNT> 
<SNT> parentCompany publisher </SNT> 
<SNT> country capital </SNT> 
<SNT> country capital </SNT> 
<SNT> country ethnicGroup </SNT> 
<SNT> country ethnicGroup </SNT> 
<SNT> country ethnicGroup </SNT> 
<SNT> operatingOrganisation </SNT> <SNT> runwayName runwayLength </SNT> 
<SNT> cityServed cityServed runwayLength </SNT> 
<SNT> location isPartOf country </SNT> 
<SNT> location ICAO_Location_Identifier </SNT> <SNT> operatingOrganisation </SNT> 
<SNT> location operatingOrganisation runwayLength </SNT> 
<SNT> operatingOrganisation aircraftFighter battles </SNT> 
<SNT> operatingOrganisation battles aircraftFighter </SNT> 
<SNT> cityServed 1st_runway_SurfaceType runwayLength </SNT> 
<SNT> location operatingOrganisation headquarter </SNT> 
<SNT> location runwayName runwayLength </SNT> 
<SNT> elevationAboveTheSeaLevel_(in_metres) location country </SNT> 
<SNT> location elevationAboveTheSeaLevel_(in_metres) runwayLength </SNT> 
<SNT> location elevationAboveTheSeaLevel_(in_metres) runwayLength </SNT> 
<SNT> location capital language </SNT> 
<SNT> location country demonym </SNT> 
<SNT> cityServed country language </SNT> 
<SNT> location isPartOf country </SNT> 
<SNT> operatingOrganisation regionServed headquarter </SNT> 
<SNT> location country </SNT> <SNT> runwayLength </SNT> 
<SNT> cityServed country leaderName </SNT> 
<SNT> location leaderParty </SNT> 
<SNT> location country leaderParty </SNT> 
<SNT> location country leaderParty </SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> birthDate </SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> birthPlace leaderName birthPlace </SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> birthPlace </SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> birthPlace deathPlace </SNT> 
<SNT> nationality birthPlace deathPlace </SNT> 
<SNT> nationality birthPlace deathPlace </SNT> 
<SNT> birthPlace leaderName deathPlace </SNT> 
<SNT> deathPlace leaderName </SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> birthPlace </SNT> 
<SNT> birthPlace </SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> birthPlace ethnicGroup birthPlace </SNT> 
<SNT> birthPlace nationality status </SNT> 
<SNT> birthPlace nationality leaderName </SNT> 
<SNT> nationality almaMater was_selected_by_NASA </SNT> 
<SNT> nationality birthPlace almaMater </SNT> 
<SNT> almaMater was_selected_by_NASA deathPlace </SNT> 
<SNT> almaMater deathPlace leaderName </SNT> 
<SNT> deathPlace deathDate </SNT> 
<SNT> birthPlace bird timeInSpace </SNT> 
<SNT> club club </SNT> 
<SNT> manager club club </SNT> 
<SNT> club club </SNT> 
<SNT> club club </SNT> 
<SNT> club club </SNT> 
<SNT> club club </SNT> <SNT> ground </SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> birthPlace birthDate </SNT> 
<SNT> club club </SNT> 
<SNT> club club </SNT> <SNT> ground </SNT> 
<SNT> club club </SNT> 
<SNT> club club </SNT> <SNT> ground </SNT> 
<SNT> birthPlace birthDate </SNT> <SNT> club </SNT> 
<SNT> birthPlace birthDate </SNT> 
<SNT> birthPlace deathPlace leaderName </SNT> 
<SNT> club club </SNT> 
<SNT> club club </SNT> 
<SNT> club club </SNT> <SNT> ground </SNT> 
<SNT> club club </SNT> <SNT> ground </SNT> 
<SNT> manager club club </SNT> 
<SNT> manager club club </SNT> 
<SNT> club club </SNT> 
<SNT> birthPlace club </SNT> <SNT> club </SNT> 
<SNT> birthPlace height club </SNT> 
<SNT> birthPlace height club </SNT> 
<SNT> club league </SNT> 
<SNT> club league </SNT> 
<SNT> club club </SNT> <SNT> ground </SNT> 
<SNT> ground club </SNT> 
<SNT> club </SNT> <SNT> manager season </SNT> 
<SNT> manager club club </SNT> 
<SNT> birthPlace deathPlace leaderName </SNT> 
<SNT> manager club club </SNT> 
<SNT> manager club </SNT> <SNT> club </SNT> 
<SNT> birthPlace isPartOf leaderName </SNT> 
<SNT> city </SNT> 
<SNT> club club </SNT> <SNT> ground </SNT> 
<SNT> location architect birthPlace </SNT> 
<SNT> location isPartOf leaderName </SNT> 
<SNT> location floorArea floorCount </SNT> 
<SNT> location isPartOf leaderName </SNT> 
<SNT> country architect floorCount </SNT> 
<SNT> architect completionDate location </SNT> 
<SNT> floorCount location tenant </SNT> 
<SNT> floorCount architect tenant </SNT> 
<SNT> birthPlace architect significantBuilding </SNT> 
<SNT> architect completionDate owner </SNT> 
<SNT> location completionDate architecturalStyle </SNT> 
<SNT> location completionDate architecturalStyle </SNT> 
<SNT> location country leaderName </SNT> 
<SNT> country capital currency </SNT> 
<SNT> buildingStartDate country currentTenants </SNT> 
<SNT> birthPlace architect significantBuilding </SNT> 
<SNT> birthPlace significantBuilding architect </SNT> 
<SNT> location added_to_the_National_Register_of_Historic_Places ReferenceNumber_in_the_National_Register_of_Historic_Places </SNT> 
<SNT> yearOfConstruction location added_to_the_National_Register_of_Historic_Places </SNT> 
<SNT> yearOfConstruction location added_to_the_National_Register_of_Historic_Places </SNT> 
<SNT> architect hometown leaderName </SNT> 
<SNT> birthPlace architect </SNT> <SNT> leaderName </SNT> 
<SNT> leaderName country location </SNT> 
<SNT> location location leaderName </SNT> 
<SNT> keyPerson location tenant </SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> formerName </SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> nationality almaMater </SNT> 
<SNT> deathPlace </SNT> 
<SNT> deathPlace </SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> birthPlace nationality </SNT> 
<SNT> formerName </SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> formerName </SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> isPartOf country ethnicGroup </SNT> 
<SNT> leaderTitle </SNT> 
<SNT> isPartOf country ethnicGroup </SNT> 
<SNT> isPartOf country ethnicGroup </SNT> 
<SNT> country ethnicGroup capital </SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> location isPartOf leaderTitle </SNT> 
<SNT> isPartOf country isPartOf </SNT> 
<SNT> isPartOf country ethnicGroup </SNT> 
<SNT> isPartOf isPartOf leaderTitle </SNT> 
<SNT> countySeat isPartOf country </SNT> 
<SNT> country ethnicGroup capital </SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> country largestCity isPartOf </SNT> 
<SNT> country largestCity isPartOf </SNT> 
<SNT> isPartOf country isPartOf </SNT> 
<SNT> country isPartOf capital </SNT> 
<SNT> capital country isPartOf </SNT> 
<SNT> isPartOf capital isPartOf </SNT> 
<SNT> country largestCity isPartOf </SNT> 
<SNT> capital country isPartOf </SNT> 
<SNT> isPartOf largestCity language </SNT> 
<SNT> country isPartOf capital </SNT> 
<SNT> country isPartOf capital </SNT> 
<SNT> country capital language </SNT> 
<SNT> isPartOf country ethnicGroup </SNT> 
<SNT> isPartOf country ethnicGroup </SNT> 
<SNT> country leader ethnicGroup </SNT> 
<SNT> country language ethnicGroup </SNT> 
<SNT> country language ethnicGroup </SNT> 
<SNT> creator creator nationality </SNT> 
<SNT> broadcastedBy starring lastAired </SNT> 
<SNT> distributor starring firstAppearanceInFilm </SNT> 
<SNT> distributor starring </SNT> <SNT> series </SNT> 
<SNT> distributor starring </SNT> <SNT> series </SNT> 
<SNT> alternativeName creator creator </SNT> 
<SNT> alternativeName creator creator </SNT> 
<SNT> alternativeName creator creator </SNT> 
<SNT> ingredient region country </SNT> 
<SNT> region mainIngredients </SNT> 
<SNT> division family </SNT> <SNT> ingredient </SNT> 
<SNT> ingredient region country </SNT> 
<SNT> region country ingredient </SNT> 
<SNT> mainIngredients region country </SNT> 
<SNT> country capital leaderName </SNT> 
<SNT> country region ethnicGroup </SNT> 
<SNT> dishVariation dishVariation ingredient </SNT> 
<SNT> country leaderName ethnicGroup </SNT> 
<SNT> country leaderName capital </SNT> 
<SNT> ingredient dishVariation dishVariation </SNT> 
<SNT> ingredient country region </SNT> 
<SNT> country leaderName currency </SNT> 
<SNT> ingredient region country </SNT> 
<SNT> dishVariation course ingredient </SNT> 
<SNT> course region </SNT> <SNT> mainIngredients </SNT> 
<SNT> dishVariation region </SNT> <SNT> mainIngredients </SNT> 
<SNT> ingredient region </SNT> 
<SNT> ingredient </SNT> <SNT> region country </SNT> 
<SNT> region country ingredient </SNT> 
<SNT> country leaderName leaderName </SNT> 
<SNT> country mainIngredients </SNT> 
<SNT> country mainIngredients </SNT> 
<SNT> region country leaderName </SNT> 
<SNT> region country ingredient </SNT> 
<SNT> ingredient region country </SNT> 
<SNT> country currency leaderName </SNT> 
<SNT> ingredient course </SNT> <SNT> dishVariation </SNT> 
<SNT> dishVariation course ingredient </SNT> 
<SNT> ingredient course </SNT> <SNT> dishVariation </SNT> 
<SNT> order mainIngredients </SNT> <SNT> ingredient </SNT> 
<SNT> ingredient country course </SNT> 
<SNT> region country ingredient </SNT> 
<SNT> country currency leaderName </SNT> 
<SNT> country capital leaderName </SNT> 
<SNT> language country </SNT> <SNT> ethnicGroup </SNT> 
<SNT> country leaderName language </SNT> 
<SNT> family ingredient </SNT> 
<SNT> alternativeName </SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> owner location operator </SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> foundationPlace </SNT> 
<SNT> demonym ethnicGroup </SNT> 
<SNT> capital ethnicGroup </SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> capital </SNT> 
<SNT> capital </SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> status </SNT> 
<SNT> foundationPlace </SNT> 
<SNT> keyPerson </SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> ethnicGroup leaderTitle </SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> location foundationPlace </SNT> 
<SNT> owner location </SNT> 
<SNT> owner location operator </SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> headquarter </SNT> 
<SNT> location </SNT> 
<SNT> locationCity </SNT> 
<SNT> demonym ethnicGroup </SNT> 
<SNT> location has_to_its_southeast country </SNT> 
<SNT> municipality location has_to_its_southeast </SNT> 
<SNT> nativeName location capital </SNT> 
<SNT> location legislature leader </SNT> 
<SNT> location capital leaderName </SNT> 
<SNT> award birthPlace battles </SNT> 
<SNT> birthPlace almaMater </SNT> 
<SNT> birthPlace </SNT> 
<SNT> birthPlace residence </SNT> 
<SNT> birthPlace almaMater </SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> birthPlace birthPlace </SNT> 
<SNT> birthPlace birthPlace </SNT> 
<SNT> battles commander </SNT> 
<SNT> birthPlace </SNT> 
<SNT> battles commander </SNT> 
<SNT> birthPlace almaMater </SNT> 
<SNT> birthPlace battles commander </SNT> 
<SNT> battles commander </SNT> 
<SNT> birthPlace birthPlace </SNT> 
<SNT> birthPlace birthPlace </SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> nationality leaderName </SNT> 
<SNT> nationality leaderName </SNT> 
<SNT> birthPlace almaMater </SNT> 
<SNT> battles </SNT> 
<SNT> battles </SNT> 
<SNT> birthDate </SNT> 
<SNT> birthPlace </SNT> 
<SNT> birthPlace deathPlace ethnicGroup </SNT> 
<SNT> birthPlace deathPlace </SNT> 
<SNT> birthPlace deathPlace </SNT> 
<SNT> leaderName governingBody deathPlace </SNT> 
<SNT> birthPlace </SNT> 
<SNT> birthPlace deathPlace </SNT> 
<SNT> birthPlace </SNT> 
<SNT> birthPlace </SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> region largestCity </SNT> 
<SNT> birthDate leaderName </SNT> 
<SNT> birthPlace leaderName </SNT> 
<SNT> birthPlace battles commander </SNT> 
<SNT> battles commander </SNT> 
<SNT> birthPlace deathPlace leaderName </SNT> 
<SNT> birthPlace deathPlace leaderName </SNT> 
<SNT> deathPlace leaderName </SNT> 
<SNT> battles commander </SNT> 
<SNT> nationality birthPlace currency </SNT> 
<SNT> nationality ethnicGroup language </SNT> 
<SNT> manager club club </SNT> 
<SNT> manager club club </SNT> 
<SNT> fullname numberOfMembers league </SNT> 
<SNT> manager club club </SNT> 
<SNT> ground </SNT> <SNT> numberOfMembers chairman </SNT> 
<SNT> fullname </SNT> <SNT> numberOfMembers ground </SNT> 
<SNT> manager club club </SNT> 
<SNT> manager club club </SNT> 
<SNT> fullname ground numberOfMembers </SNT> 
<SNT> league champions </SNT> <SNT> manager </SNT> 
<SNT> manager club club </SNT> 
<SNT> ground league champions </SNT> 
<SNT> manager club </SNT> <SNT> club </SNT> 
<SNT> part </SNT> <SNT> ground leader </SNT> 
<SNT> part </SNT> <SNT> ground leader </SNT> 
<SNT> club club </SNT> <SNT> manager </SNT> 
<SNT> ground league country </SNT> 
<SNT> manager club </SNT> <SNT> club </SNT> 
<SNT> manager club club </SNT> 
<SNT> club club manager </SNT> 
<SNT> club club </SNT> <SNT> manager </SNT> 
<SNT> academicStaffSize state country </SNT> 
<SNT> city affiliation numberOfPostgraduateStudents </SNT> 
<SNT> state has_to_its_northwest </SNT> <SNT> leaderName </SNT> 
<SNT> country anthem ethnicGroup </SNT> 
<SNT> country leaderName ethnicGroup </SNT> 
<SNT> established affiliation headquarters </SNT> 
<SNT> established city country </SNT> 
<SNT> author mediaType numberOfPages </SNT> 
<SNT> precededBy language </SNT> <SNT> spokenIn </SNT> 
<SNT> leaderName publisher headquarter </SNT> 
<SNT> academicDiscipline academicDiscipline ISSN_number </SNT> 
<SNT> academicDiscipline ISSN_number </SNT> 
<SNT> editor residence almaMater </SNT> 
<SNT> author OCLC_number ISBN_number </SNT> 
<SNT> author OCLC_number ISBN_number </SNT> 
<SNT> language publisher country </SNT> 
<SNT> country language </SNT> <SNT> spokenIn </SNT> 
<SNT> numberOfPages mediaType OCLC_number </SNT> 
<SNT> abbreviation ISSN_number LCCN_number </SNT> 
<SNT> language country </SNT> <SNT> followedBy </SNT> 
<SNT> author followedBy language </SNT> 
<SNT> author mediaType numberOfPages </SNT> 
<SNT> abbreviation </SNT> <SNT> LCCN_number ISSN_number </SNT> 
<SNT> abbreviation ISSN_number academicDiscipline </SNT> 
<SNT> parentCompany publisher ISSN_number </SNT> 
<SNT> state affiliation publisher </SNT> 
<SNT> language followedBy author </SNT> 
<SNT> country language spokenIn </SNT> 
<SNT> mediaType numberOfPages </SNT> <SNT> OCLC_number </SNT> 
<SNT> country language spokenIn </SNT> 
<SNT> birthPlace author deathPlace </SNT> 
<SNT> founder publisher ISSN_number </SNT> 
<SNT> country ethnicGroup capital </SNT> 
<SNT> language country </SNT> <SNT> ethnicGroup </SNT> 
<SNT> country ethnicGroup capital </SNT> 
<SNT> country leaderName ethnicGroup </SNT> 
<SNT> country leaderTitle ethnicGroup </SNT> 
<SNT> country leaderTitle ethnicGroup </SNT> 
<SNT> cityServed runwayName runwayLength elevationAboveTheSeaLevel_(in_metres) </SNT> 
<SNT> cityServed cityServed </SNT> <SNT> elevationAboveTheSeaLevel_(in_metres) runwayLength </SNT> 
<SNT> location </SNT> <SNT> operatingOrganisation runwayName runwayLength </SNT> 
<SNT> location operatingOrganisation </SNT> <SNT> IATA_Location_Identifier ICAO_Location_Identifier </SNT> 
<SNT> location operatingOrganisation </SNT> <SNT> runwayName runwayLength </SNT> 
<SNT> operatingOrganisation location </SNT> <SNT> runwayLength runwayName </SNT> 
<SNT> operatingOrganisation </SNT> <SNT> battles </SNT> <SNT> attackAircraft aircraftFighter </SNT> 
<SNT> cityServed location </SNT> <SNT> operatingOrganisation runwayLength </SNT> 
<SNT> cityServed elevationAboveTheSeaLevel_(in_metres) </SNT> <SNT> runwayName runwayLength </SNT> 
<SNT> cityServed elevationAboveTheSeaLevel_(in_metres) </SNT> <SNT> runwayName runwayLength </SNT> 
<SNT> cityServed location elevationAboveTheSeaLevel_(in_metres) runwayLength </SNT> 
<SNT> location country demonym capital </SNT> 
<SNT> location country </SNT> <SNT> capital language </SNT> 
<SNT> cityServed location isPartOf country </SNT> 
<SNT> owner operatingOrganisation </SNT> <SNT> elevationAboveTheSeaLevel_(in_metres) runwayLength </SNT> 
<SNT> operatingOrganisation headquarter hubAirport headquarter </SNT> 
<SNT> location cityServed elevationAboveTheSeaLevel_(in_metres) </SNT> <SNT> runwayLength </SNT> 
<SNT> country leaderName cityServed location </SNT> 
<SNT> owner location country leaderName </SNT> 
<SNT> cityServed </SNT> <SNT> country </SNT> <SNT> leaderName language </SNT> 
<SNT> leaderName language </SNT> <SNT> country cityServed </SNT> 
<SNT> cityServed isPartOf isPartOf country </SNT> 
<SNT> cityServed country </SNT> <SNT> leaderName language </SNT> 
<SNT> cityServed country </SNT> <SNT> language leaderName </SNT> 
<SNT> cityServed location country leaderName </SNT> 
<SNT> 1st_runway_SurfaceType division </SNT> <SNT> order class </SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> birthPlace birthDate </SNT> 
<SNT> birthPlace birthDate </SNT> 
<SNT> birthPlace occupation </SNT> 
<SNT> birthPlace occupation </SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> ethnicGroup </SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> birthPlace ethnicGroup </SNT> 
<SNT> birthPlace </SNT> 
<SNT> birthPlace </SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> birthPlace birthPlace deathPlace </SNT> 
<SNT> leaderName demonym </SNT> <SNT> country deathPlace </SNT> 
<SNT> birthPlace </SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> location </SNT> 
<SNT> location </SNT> 
<SNT> birthPlace </SNT> 
<SNT> birthPlace </SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> location </SNT> 
<SNT> birthPlace occupation </SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> deathPlace isPartOf </SNT> <SNT> leaderName deathPlace </SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> birthPlace isPartOf leaderName birthPlace </SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> birthPlace ethnicGroup </SNT> 
<SNT> nationality birthPlace </SNT> <SNT> was_selected_by_NASA timeInSpace </SNT> 
<SNT> was_a_crew_member_of operator </SNT> <SNT> awards higher </SNT> 
<SNT> birthPlace representative </SNT> <SNT> was_a_crew_member_of crewMembers </SNT> 
<SNT> birthPlace almaMater </SNT> <SNT> was_a_crew_member_of status </SNT> 
<SNT> birthPlace occupation was_selected_by_NASA was_a_crew_member_of </SNT> 
<SNT> birthName was_a_crew_member_of operator awards </SNT> 
<SNT> birthPlace deathPlace </SNT> <SNT> awards higher </SNT> 
<SNT> deathPlace </SNT> <SNT> senators awards higher </SNT> 
<SNT> nationality birthPlace almaMater </SNT> <SNT> deathPlace </SNT> 
<SNT> occupation was_selected_by_NASA was_a_crew_member_of operator </SNT> 
<SNT> manager </SNT> <SNT> club league club </SNT> 
<SNT> manager club club </SNT> 
<SNT> manager club club </SNT> 
<SNT> manager club league </SNT> 
<SNT> birthPlace birthDate </SNT> 
<SNT> city club </SNT> 
<SNT> birthDate </SNT> 
<SNT> birthPlace birthDate </SNT> 
<SNT> birthPlace birthDate </SNT> 
<SNT> club club </SNT> <SNT> manager ground </SNT> 
<SNT> club club </SNT> <SNT> manager ground </SNT> 
<SNT> club club </SNT> <SNT> manager ground </SNT> 
<SNT> club club </SNT> <SNT> height </SNT> 
<SNT> club club </SNT> <SNT> height </SNT> 
<SNT> club club </SNT> <SNT> height </SNT> 
<SNT> club club </SNT> <SNT> height </SNT> 
<SNT> birthPlace height </SNT> <SNT> club club </SNT> 
<SNT> birthPlace birthDate </SNT> <SNT> club </SNT> 
<SNT> birthPlace height club </SNT> 
<SNT> birthPlace leaderName language anthem </SNT> 
<SNT> birthPlace leaderName </SNT> <SNT> ethnicGroup anthem </SNT> 
<SNT> birthPlace leaderName language anthem </SNT> 
<SNT> club club </SNT> <SNT> manager ground </SNT> 
<SNT> manager </SNT> <SNT> club season </SNT> <SNT> chairman </SNT> 
<SNT> city city </SNT> 
<SNT> manager club league </SNT> 
<SNT> location floorCount buildingStartDate completionDate </SNT> 
<SNT> location capital ethnicGroup ethnicGroup </SNT> 
<SNT> location isPartOf country governingBody </SNT> 
<SNT> cost location floorCount floorArea </SNT> 
<SNT> location isPartOf country </SNT> <SNT> leaderName </SNT> 
<SNT> location floorCount floorArea </SNT> <SNT> completionDate </SNT> 
<SNT> location isPartOf isPartOf owner </SNT> 
<SNT> location floorCount architect </SNT> <SNT> tenant </SNT> 
<SNT> location location isPartOf </SNT> <SNT> governmentType </SNT> 
<SNT> ethnicGroup country location isPartOf </SNT> 
<SNT> location isPartOf country </SNT> <SNT> ethnicGroup </SNT> 
<SNT> location location country </SNT> <SNT> ethnicGroup </SNT> 
<SNT> location location country ethnicGroup </SNT> 
<SNT> owner </SNT> <SNT> address location completionDate </SNT> 
<SNT> location country leaderName leaderName </SNT> 
<SNT> location yearOfConstruction </SNT> <SNT> added_to_the_National_Register_of_Historic_Places ReferenceNumber_in_the_National_Register_of_Historic_Places </SNT> 
<SNT> yearOfConstruction location </SNT> <SNT> added_to_the_National_Register_of_Historic_Places ReferenceNumber_in_the_National_Register_of_Historic_Places </SNT> 
<SNT> location isPartOf isPartOf leaderName </SNT> 
<SNT> location isPartOf country </SNT> <SNT> leaderName </SNT> 
<SNT> architect birthPlace </SNT> <SNT> postalCode leaderName </SNT> 
<SNT> postalCode leaderName birthPlace architect </SNT> 
<SNT> leaderName location country leaderName </SNT> 
<SNT> location country </SNT> <SNT> leaderName leaderName </SNT> 
<SNT> location location country ethnicGroup </SNT> 
<SNT> leaderName capital leaderTitle </SNT> <SNT> location </SNT> 
<SNT> leaderName leaderTitle </SNT> <SNT> language location </SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> formerName </SNT> 
<SNT> birthPlace almaMater </SNT> 
<SNT> birthPlace almaMater </SNT> 
<SNT> almaMater deathPlace </SNT> 
<SNT> almaMater deathPlace </SNT> 
<SNT> nationality deathPlace </SNT> 
<SNT> nationality deathPlace </SNT> 
<SNT> birthPlace deathPlace </SNT> 
<SNT> birthPlace deathPlace </SNT> 
<SNT> formerName </SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> formerName </SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> formerName </SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> country isPartOf </SNT> <SNT> ethnicGroup language </SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> isPartOf isPartOf country </SNT> 
<SNT> isPartOf country </SNT> <SNT> capital ethnicGroup </SNT> 
<SNT> state country </SNT> <SNT> capital isPartOf </SNT> 
<SNT> location isPartOf </SNT> <SNT> leaderTitle language </SNT> 
<SNT> isPartOf </SNT> 
<SNT> isPartOf isPartOf country </SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> leaderTitle </SNT> 
<SNT> state country ethnicGroup isPartOf </SNT> 
<SNT> isPartOf country ethnicGroup state </SNT> 
<SNT> country isPartOf capital isPartOf </SNT> 
<SNT> country isPartOf capital isPartOf </SNT> 
<SNT> isPartOf largestCity isPartOf </SNT> 
<SNT> location isPartOf language </SNT> <SNT> leaderTitle </SNT> 
<SNT> isPartOf isPartOf country </SNT> 
<SNT> capital isPartOf country </SNT> <SNT> isPartOf </SNT> 
<SNT> country isPartOf </SNT> <SNT> language largestCity </SNT> 
<SNT> isPartOf country </SNT> <SNT> capital ethnicGroup </SNT> 
<SNT> country isPartOf capital </SNT> <SNT> largestCity </SNT> 
<SNT> country ethnicGroup leaderTitle leader </SNT> 
<SNT> country leaderTitle leader </SNT> <SNT> ethnicGroup </SNT> 
<SNT> country isPartOf capital </SNT> <SNT> ethnicGroup </SNT> 
<SNT> country ethnicGroup </SNT> <SNT> leaderTitle leader </SNT> 
<SNT> country ethnicGroup </SNT> <SNT> language isPartOf </SNT> 
<SNT> country language isPartOf </SNT> <SNT> ethnicGroup </SNT> 
<SNT> country language isPartOf </SNT> <SNT> ethnicGroup </SNT> 
<SNT> locationCity keyPerson broadcastedBy product </SNT> 
<SNT> starring creator </SNT> <SNT> broadcastedBy firstAired </SNT> 
<SNT> nationality creator series starring </SNT> 
<SNT> alternativeName creator creator nationality </SNT> 
<SNT> mainIngredients region country </SNT> 
<SNT> country capital leaderName leaderName </SNT> 
<SNT> region country </SNT> <SNT> mainIngredients </SNT> 
<SNT> region country </SNT> <SNT> mainIngredients </SNT> 
<SNT> region country </SNT> <SNT> leaderName ethnicGroup </SNT> 
<SNT> country region </SNT> <SNT> ethnicGroup ethnicGroup </SNT> 
<SNT> region country </SNT> <SNT> ethnicGroup ethnicGroup </SNT> 
<SNT> country </SNT> <SNT> leaderName capital ethnicGroup </SNT> 
<SNT> country </SNT> <SNT> leaderName capital ethnicGroup </SNT> 
<SNT> dishVariation dishVariation ingredient country </SNT> 
<SNT> country mainIngredients </SNT> <SNT> alternativeName </SNT> 
<SNT> dishVariation course </SNT> <SNT> country ethnicGroup </SNT> 
<SNT> mainIngredients </SNT> <SNT> region country </SNT> 
<SNT> mainIngredients region country </SNT> 
<SNT> region country </SNT> <SNT> mainIngredients </SNT> 
<SNT> dishVariation region isPartOf </SNT> <SNT> ingredient </SNT> 
<SNT> country region </SNT> <SNT> mainIngredients </SNT> 
<SNT> region </SNT> <SNT> country mainIngredients </SNT> 
<SNT> region country </SNT> <SNT> mainIngredients </SNT> 
<SNT> region country </SNT> <SNT> mainIngredients </SNT> 
<SNT> country language </SNT> <SNT> leaderName leaderName </SNT> 
<SNT> country </SNT> <SNT> dishVariation dishVariation ingredient </SNT> 
<SNT> region country </SNT> <SNT> mainIngredients </SNT> 
<SNT> course country </SNT> <SNT> mainIngredients </SNT> 
<SNT> country region </SNT> <SNT> mainIngredients </SNT> 
<SNT> country region </SNT> <SNT> language leaderName </SNT> 
<SNT> region country </SNT> <SNT> mainIngredients ingredient </SNT> 
<SNT> alternativeName region country ingredient </SNT> 
<SNT> alternativeName region </SNT> <SNT> mainIngredients </SNT> 
<SNT> ingredient mainIngredients </SNT> <SNT> order division </SNT> 
<SNT> region country </SNT> <SNT> mainIngredients ingredient </SNT> 
<SNT> ingredient course country region </SNT> 
<SNT> region country </SNT> <SNT> mainIngredients </SNT> 
<SNT> course dishVariation </SNT> <SNT> country leaderName </SNT> 
<SNT> ingredient region country leaderName </SNT> 
<SNT> region country ethnicGroup </SNT> <SNT> leaderName </SNT> 
<SNT> ethnicGroup ethnicGroup language country </SNT> 
<SNT> ethnicGroup ethnicGroup language country </SNT> 
<SNT> dishVariation course country language </SNT> 
<SNT> dishVariation </SNT> <SNT> course country language </SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> owner location operator </SNT> 
<SNT> owner operator </SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> foundationPlace </SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> alternativeName </SNT> 
<SNT> alternativeName class </SNT> 
<SNT> class </SNT> 
<SNT> class </SNT> 
<SNT> status </SNT> 
<SNT> demonym leaderName </SNT> 
<SNT> status </SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> status </SNT> 
<SNT> foundationPlace </SNT> 
<SNT> foundationPlace </SNT> 
<SNT> keyPerson </SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> capital </SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> class </SNT> 
<SNT> foundedBy division </SNT> 
<SNT> location owner foundationPlace </SNT> 
<SNT> owner location operator </SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> locationCity parentCompany </SNT> 
<SNT> country leaderName locationCity </SNT> 
<SNT> language capital ethnicGroup </SNT> 
<SNT> demonym capital ethnicGroup </SNT> 
<SNT> established state country category </SNT> 
<SNT> location capital leaderTitle leaderName </SNT> 
<SNT> nativeName dedicatedTo </SNT> <SNT> location capital </SNT> 
<SNT> established district location nearestCity </SNT> 
<SNT> material location capital leaderName </SNT> 
<SNT> designer inaugurationDate location leaderTitle </SNT> 
<SNT> birthPlace award </SNT> <SNT> deathPlace battles </SNT> 
<SNT> birthPlace award </SNT> <SNT> deathPlace battles </SNT> 
<SNT> birthPlace almaMater affiliation </SNT> 
<SNT> birthPlace residence region </SNT> 
<SNT> birthPlace residence region </SNT> 
<SNT> birthPlace residence </SNT> 
<SNT> birthPlace residence </SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> birthPlace </SNT> 
<SNT> birthPlace birthPlace </SNT> 
<SNT> nationality birthPlace ethnicGroup </SNT> 
<SNT> birthPlace almaMater battles </SNT> 
<SNT> nationality battles </SNT> <SNT> language commander </SNT> 
<SNT> battles deathPlace commander </SNT> 
<SNT> birthPlace birthPlace </SNT> 
<SNT> birthPlace birthPlace </SNT> 
<SNT> birthPlace birthPlace </SNT> 
<SNT> nationality leaderName currency </SNT> 
<SNT> nationality leaderName </SNT> 
<SNT> nationality leaderName leaderName </SNT> 
<SNT> nationality leaderName </SNT> 
<SNT> birthPlace battles </SNT> 
<SNT> birthPlace battles </SNT> 
<SNT> battles commander </SNT> 
<SNT> birthPlace deathPlace birthPlace </SNT> 
<SNT> birthPlace </SNT> 
<SNT> birthPlace battles </SNT> <SNT> language commander </SNT> 
<SNT> birthPlace </SNT> <SNT> battles commander </SNT> <SNT> deathPlace </SNT> 
<SNT> birthPlace deathPlace deathPlace </SNT> 
<SNT> deathPlace leaderName </SNT> 
<SNT> birthPlace </SNT> 
<SNT> birthPlace </SNT> 
<SNT> birthPlace battles </SNT> 
<SNT> birthPlace </SNT> 
<SNT> birthPlace </SNT> 
<SNT> birthPlace </SNT> 
<SNT> birthPlace </SNT> 
<SNT> largestCity region state </SNT> 
<SNT> region leaderName largestCity </SNT> 
<SNT> award battles commander </SNT> 
<SNT> nationality language </SNT> 
<SNT> birthPlace </SNT> 
<SNT> club club </SNT> <SNT> club manager </SNT> 
<SNT> placeOfBirth club </SNT> <SNT> manager club </SNT> 
<SNT> placeOfBirth manager club club </SNT> 
<SNT> ground numberOfMembers </SNT> <SNT> league chairman </SNT> 
<SNT> club manager ground leader </SNT> 
<SNT> club manager ground </SNT> <SNT> leader </SNT> 
<SNT> fullname season ground numberOfMembers </SNT> 
<SNT> fullname </SNT> <SNT> numberOfMembers </SNT> <SNT> ground season </SNT> 
<SNT> club club </SNT> <SNT> club manager </SNT> 
<SNT> numberOfMembers ground </SNT> <SNT> league season </SNT> 
<SNT> ground operator </SNT> <SNT> owner tenant </SNT> 
<SNT> fullname nickname </SNT> <SNT> season numberOfMembers </SNT> 
<SNT> manager club club club </SNT> 
<SNT> ground location </SNT> <SNT> league champions </SNT> 
<SNT> ground location country </SNT> <SNT> league </SNT> 
<SNT> ground location country </SNT> <SNT> league </SNT> 
<SNT> ground city isPartOf country </SNT> 
<SNT> leader leader capital </SNT> <SNT> ground </SNT> 
<SNT> manager club club club </SNT> 
<SNT> city country established </SNT> <SNT> river </SNT> 
<SNT> campus country </SNT> <SNT> affiliation city </SNT> 
<SNT> city country director affiliation </SNT> 
<SNT> was_given_the_'Technical_Campus'_status_by location </SNT> <SNT> sportsOffered sportsGoverningBody </SNT> 
<SNT> city isPartOf country </SNT> <SNT> capital </SNT> 
<SNT> city country governmentType leaderName </SNT> 
<SNT> city state academicStaffSize </SNT> <SNT> leaderName </SNT> 
<SNT> country </SNT> <SNT> capital anthem ethnicGroup </SNT> 
<SNT> city country </SNT> <SNT> capital leaderName </SNT> 
<SNT> birthPlace author </SNT> <SNT> precededBy </SNT> 
<SNT> author numberOfPages mediaType ISBN_number </SNT> 
<SNT> leaderName country publisher parentCompany </SNT> 
<SNT> author mediaType OCLC_number ISBN_number </SNT> 
<SNT> author mediaType OCLC_number ISBN_number </SNT> 
<SNT> country ethnicGroup </SNT> <SNT> language spokenIn </SNT> 
<SNT> publisher country </SNT> <SNT> language ethnicGroup </SNT> 
<SNT> author numberOfPages </SNT> <SNT> mediaType ISBN_number </SNT> 
<SNT> abbreviation </SNT> <SNT> academicDiscipline </SNT> <SNT> LCCN_number ISSN_number </SNT> 
<SNT> academicDiscipline abbreviation </SNT> <SNT> LCCN_number ISSN_number </SNT> 
<SNT> publisher </SNT> <SNT> abbreviation LCCN_number ISSN_number </SNT> 
<SNT> state affiliation publisher </SNT> <SNT> affiliation </SNT> 
<SNT> city state </SNT> <SNT> affiliation publisher </SNT> 
<SNT> author numberOfPages mediaType ISBN_number </SNT> 
<SNT> language spokenIn </SNT> <SNT> country ethnicGroup </SNT> 
<SNT> firstPublicationYear publisher country </SNT> <SNT> parentCompany </SNT> 
<SNT> country ethnicGroup </SNT> <SNT> language spokenIn </SNT> 
<SNT> language spokenIn country leaderName </SNT> 
<SNT> country language </SNT> <SNT> leaderName </SNT> <SNT> ethnicGroup </SNT> 
<SNT> country leaderName language spokenIn </SNT> 
<SNT> country leaderName </SNT> <SNT> language ethnicGroup </SNT> 
<SNT> leaderName language </SNT> <SNT> country isPartOf location </SNT> 
<SNT> location operatingOrganisation </SNT> <SNT> elevationAboveTheSeaLevel_(in_metres) runwayLength runwayName </SNT> 
<SNT> location operatingOrganisation </SNT> <SNT> elevationAboveTheSeaLevel_(in_metres) runwayLength </SNT> <SNT> ICAO_Location_Identifier </SNT> 
<SNT> operatingOrganisation location </SNT> <SNT> ICAO_Location_Identifier </SNT> <SNT> runwayName runwayLength </SNT> 
<SNT> operatingOrganisation location ICAO_Location_Identifier </SNT> <SNT> runwayName runwayLength </SNT> 
<SNT> operatingOrganisation attackAircraft </SNT> <SNT> transportAircraft aircraftFighter </SNT> <SNT> battles </SNT> 
<SNT> operatingOrganisation attackAircraft </SNT> <SNT> battles battles </SNT> 
<SNT> cityServed elevationAboveTheSeaLevel_(in_metres) </SNT> <SNT> 1st_runway_SurfaceType runwayLength runwayName </SNT> 
<SNT> cityServed location operatingOrganisation </SNT> <SNT> runwayLength runwayName </SNT> 
<SNT> cityServed location </SNT> <SNT> operatingOrganisation </SNT> <SNT> runwayName runwayLength </SNT> 
<SNT> location </SNT> <SNT> cityServed country elevationAboveTheSeaLevel_(in_metres) runwayLength </SNT> 
<SNT> cityServed location </SNT> <SNT> runwayName runwayLength elevationAboveTheSeaLevel_(in_metres) </SNT> 
<SNT> cityServed location </SNT> <SNT> elevationAboveTheSeaLevel_(in_metres) runwayName runwayLength </SNT> 
<SNT> cityServed location country </SNT> <SNT> elevationAboveTheSeaLevel_(in_metres) runwayLength </SNT> 
<SNT> cityServed location elevationAboveTheSeaLevel_(in_metres) </SNT> <SNT> runwayName runwayLength </SNT> 
<SNT> cityServed country </SNT> <SNT> leaderName leaderName </SNT> <SNT> language </SNT> 
<SNT> cityServed country </SNT> <SNT> leaderName leaderName </SNT> <SNT> language </SNT> 
<SNT> cityServed country </SNT> <SNT> location isPartOf </SNT> <SNT> leaderName </SNT> 
<SNT> leaderName leaderName capital </SNT> <SNT> country cityServed </SNT> 
<SNT> ICAO_Location_Identifier location isPartOf isPartOf country </SNT> 
<SNT> cityServed cityServed isPartOf isPartOf </SNT> 
<SNT> cityServed cityServed isPartOf isPartOf country </SNT> 
<SNT> location isPartOf isPartOf </SNT> <SNT> operatingOrganisation aircraftHelicopter </SNT> 
<SNT> leaderName isPartOf location </SNT> <SNT> operatingOrganisation aircraftHelicopter </SNT> 
<SNT> cityServed country </SNT> <SNT> leaderName leaderName </SNT> <SNT> language </SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> birthPlace birthDate birthPlace </SNT> 
<SNT> birthDate birthPlace occupation </SNT> 
<SNT> birthDate birthPlace occupation </SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> birthPlace </SNT> 
<SNT> birthPlace </SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> birthPlace occupation </SNT> 
<SNT> birthPlace </SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> birthPlace nationality deathPlace </SNT> 
<SNT> birthPlace deathPlace </SNT> <SNT> leaderTitle leaderName </SNT> 
<SNT> birthPlace deathPlace </SNT> <SNT> leaderTitle leaderName </SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> location </SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> birthPlace birthPlace </SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> birthPlace occupation </SNT> 
<SNT> birthPlace occupation </SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> birthPlace </SNT> 
<SNT> birthPlace </SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> birthPlace </SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> birthPlace deathPlace leaderTitle leaderName </SNT> 
<SNT> birthPlace deathPlace leaderName </SNT> <SNT> ethnicGroup language </SNT> 
<SNT> location </SNT> 
<SNT> location </SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> leaderName leaderName language </SNT> <SNT> country deathPlace </SNT> 
<SNT> nationality occupation birthPlace </SNT> <SNT> was_selected_by_NASA status </SNT> 
<SNT> birthPlace was_a_crew_member_of operator </SNT> <SNT> awards higher </SNT> 
<SNT> nationality birthPlace was_a_crew_member_of operator commander </SNT> 
<SNT> nationality birthPlace birthDate </SNT> <SNT> occupation was_a_crew_member_of </SNT> 
<SNT> nationality birthPlace </SNT> <SNT> occupation was_a_crew_member_of operator </SNT> 
<SNT> birthPlace almaMater </SNT> <SNT> occupation was_a_crew_member_of </SNT> <SNT> status </SNT> 
<SNT> birthPlace </SNT> <SNT> was_a_crew_member_of operator backup_pilot </SNT> <SNT> status </SNT> 
<SNT> birthDate </SNT> <SNT> was_a_crew_member_of operator backup_pilot </SNT> <SNT> status </SNT> 
<SNT> was_a_crew_member_of operator deathPlace </SNT> <SNT> senators gemstone </SNT> 
<SNT> nationality </SNT> <SNT> birthPlace awards higher </SNT> <SNT> deathPlace </SNT> 
<SNT> nationality birthPlace was_a_crew_member_of </SNT> <SNT> occupation dateOfRetirement </SNT> 
<SNT> manager club club </SNT> 
<SNT> manager club club </SNT> 
<SNT> club club </SNT> <SNT> manager </SNT> 
<SNT> manager club club league </SNT> 
<SNT> club club </SNT> <SNT> manager </SNT> 
<SNT> manager club club </SNT> 
<SNT> club club </SNT> <SNT> manager </SNT> 
<SNT> club club </SNT> <SNT> manager </SNT> 
<SNT> club club </SNT> <SNT> manager </SNT> 
<SNT> club club </SNT> <SNT> manager </SNT> 
<SNT> club club </SNT> <SNT> manager </SNT> 
<SNT> club club </SNT> <SNT> manager </SNT> 
<SNT> completionDate location </SNT> <SNT> isPartOf isPartOf country </SNT> 
<SNT> location buildingStartDate </SNT> <SNT> floorCount floorArea architecturalStyle </SNT> 
<SNT> location isPartOf capital country </SNT> <SNT> leaderName </SNT> 
<SNT> foundationPlace tenant location </SNT> <SNT> leaderName language </SNT> 
<SNT> keyPerson foundationPlace </SNT> <SNT> tenant location language </SNT> 
<SNT> leaderName language </SNT> <SNT> location tenant foundationPlace </SNT> 
<SNT> birthPlace architect significantBuilding leaderName </SNT> 
<SNT> completionDate height </SNT> <SNT> buildingStartDate floorArea </SNT> 
<SNT> location location country </SNT> <SNT> floorCount address </SNT> 
<SNT> location isPartOf country </SNT> <SNT> leaderName ethnicGroup </SNT> 
<SNT> location country </SNT> <SNT> architect owner currentTenants </SNT> 
<SNT> location location country </SNT> <SNT> leaderTitle leaderName </SNT> 
<SNT> region state country leaderName </SNT> <SNT> leaderName </SNT> 
<SNT> country state </SNT> <SNT> leaderName governingBody </SNT> <SNT> capital </SNT> <SNT> </SNT> 
<SNT> birthPlace architect significantBuilding significantProject significantBuilding </SNT> 
<SNT> birthPlace architect significantBuilding significantProject significantBuilding </SNT> 
<SNT> location isPartOf country </SNT> <SNT> isPartOf leaderName </SNT> 
<SNT> architect birthPlace postalCode </SNT> <SNT> leaderName governingBody </SNT> 
<SNT> leaderTitle leaderName </SNT> <SNT> leaderName </SNT> <SNT> location capital </SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> nationality almaMater deathPlace </SNT> 
<SNT> almaMater deathPlace </SNT> 
<SNT> birthPlace almaMater deathPlace </SNT> 
<SNT> birthPlace almaMater </SNT> 
<SNT> birthPlace almaMater nationality </SNT> 
<SNT> nationality almaMater deathPlace </SNT> 
<SNT> nationality almaMater deathPlace </SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> nationality birthPlace deathPlace </SNT> 
<SNT> formerName </SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> country leaderTitle </SNT> <SNT> demonym ethnicGroup </SNT> 
<SNT> country isPartOf ethnicGroup </SNT> <SNT> largestCity isPartOf </SNT> 
<SNT> state country </SNT> <SNT> capital language isPartOf </SNT> 
<SNT> state country capital </SNT> <SNT> language isPartOf </SNT> 
<SNT> state country capital </SNT> <SNT> language isPartOf </SNT> 
<SNT> isPartOf isPartOf country </SNT> 
<SNT> isPartOf isPartOf language </SNT> <SNT> leaderTitle isPartOf </SNT> 
<SNT> isPartOf </SNT> 
<SNT> isPartOf isPartOf </SNT> 
<SNT> state country </SNT> <SNT> isPartOf ethnicGroup capital </SNT> 
<SNT> isPartOf isPartOf country </SNT> 
<SNT> largestCity isPartOf isPartOf language </SNT> 
<SNT> largestCity isPartOf isPartOf language </SNT> 
<SNT> largestCity isPartOf isPartOf </SNT> <SNT> language </SNT> 
<SNT> country language isPartOf </SNT> <SNT> capital isPartOf </SNT> 
<SNT> largestCity isPartOf isPartOf </SNT> <SNT> language </SNT> 
<SNT> country isPartOf capital </SNT> <SNT> largestCity isPartOf </SNT> 
<SNT> country isPartOf ethnicGroup </SNT> <SNT> capital largestCity </SNT> 
<SNT> demonym ethnicGroup capital </SNT> <SNT> country isPartOf </SNT> 
<SNT> country isPartOf demonym </SNT> <SNT> capital ethnicGroup </SNT> 
<SNT> country language isPartOf </SNT> <SNT> demonym ethnicGroup </SNT> 
<SNT> capital ethnicGroup country </SNT> <SNT> state isPartOf </SNT> 
<SNT> isPartOf country </SNT> <SNT> ethnicGroup isPartOf </SNT> 
<SNT> country isPartOf ethnicGroup </SNT> <SNT> capital largestCity </SNT> 
<SNT> country leaderTitle leader </SNT> <SNT> language isPartOf </SNT> 
<SNT> country language isPartOf </SNT> <SNT> demonym ethnicGroup </SNT> 
<SNT> country isPartOf capital </SNT> <SNT> ethnicGroup leader </SNT> 
<SNT> country leaderTitle </SNT> <SNT> demonym ethnicGroup </SNT> 
<SNT> creator nationality creator </SNT> <SNT> series starring </SNT> 
<SNT> region </SNT> <SNT> country leaderName currency language </SNT> 
<SNT> capital </SNT> <SNT> ethnicGroup ethnicGroup </SNT> <SNT> country region </SNT> 
<SNT> capital ethnicGroup ethnicGroup </SNT> <SNT> country region </SNT> 
<SNT> capital ethnicGroup ethnicGroup </SNT> <SNT> country region </SNT> 
<SNT> leaderName ethnicGroup ethnicGroup </SNT> <SNT> country region </SNT> 
<SNT> region country </SNT> <SNT> mainIngredients ingredient </SNT> 
<SNT> country </SNT> <SNT> leaderName capital language ethnicGroup </SNT> 
<SNT> country mainIngredients </SNT> <SNT> alternativeName dishVariation </SNT> 
<SNT> region country leaderName leaderName language </SNT> 
<SNT> region country </SNT> <SNT> ingredient family order </SNT> 
<SNT> leaderName leaderName language </SNT> <SNT> country ingredient </SNT> 
<SNT> country dishVariation dishVariation ingredient </SNT> <SNT> leaderName </SNT> 
<SNT> country </SNT> <SNT> dishVariation dishVariation ingredient </SNT> <SNT> leaderName </SNT> 
<SNT> country dishVariation dishVariation ingredient </SNT> <SNT> leaderName </SNT> 
<SNT> alternativeName region country </SNT> <SNT> mainIngredients </SNT> 
<SNT> leaderName demonym </SNT> <SNT> country region leaderName </SNT> 
<SNT> region country leaderName leaderName demonym </SNT> 
<SNT> course region country </SNT> <SNT> mainIngredients </SNT> 
<SNT> leaderName language country </SNT> <SNT> course dishVariation </SNT> 
<SNT> leaderName country region </SNT> <SNT> course dishVariation </SNT> 
<SNT> region country leaderName </SNT> <SNT> ethnicGroup leaderName </SNT> 
<SNT> capital leaderName leaderName </SNT> <SNT> language country </SNT> 
<SNT> country demonym capital </SNT> <SNT> leaderName leaderName </SNT> 
<SNT> leaderName leaderName capital language country </SNT> 
<SNT> ethnicGroup country </SNT> <SNT> ingredient region </SNT> 
<SNT> country ethnicGroup </SNT> <SNT> region leaderName </SNT> <SNT> language </SNT> 
<SNT> country language language </SNT> <SNT> ethnicGroup ethnicGroup </SNT> 
<SNT> region country leaderName language </SNT> <SNT> ethnicGroup </SNT> 
<SNT> leaderName demonym </SNT> <SNT> country region </SNT> <SNT> leaderName </SNT> 
<SNT> location owner </SNT> <SNT> parentCompany </SNT> <SNT> operator </SNT> 
<SNT> operator </SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> ethnicGroup demonym </SNT> <SNT> capital anthem </SNT> 
<SNT> alternativeName class </SNT> 
<SNT> owner location </SNT> 
<SNT> keyPerson </SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> headquarter locationCity operator </SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> leaderName </SNT> 
<SNT> class </SNT> 
<SNT> foundedBy division </SNT> 
<SNT> foundationPlace owner </SNT> <SNT> location foundationPlace </SNT> 
<SNT> location owner </SNT> <SNT> keyPerson foundationPlace </SNT> 
<SNT> location owner </SNT> <SNT> keyPerson foundationPlace </SNT> 
<SNT> location owner </SNT> <SNT> parentCompany </SNT> <SNT> operator </SNT> 
<SNT> demonym language </SNT> <SNT> leaderName </SNT> 
<SNT> country capital </SNT> <SNT> leaderName locationCity </SNT> 
<SNT> country capital </SNT> <SNT> leaderName locationCity </SNT> 
<SNT> ethnicGroup demonym </SNT> <SNT> capital language </SNT> 
<SNT> municipality location country </SNT> <SNT> established category </SNT> 
<SNT> category location </SNT> <SNT> has_to_its_north </SNT> <SNT> has_to_its_southeast has_to_its_southwest </SNT> 
<SNT> capital leaderTitle leaderName </SNT> <SNT> location dedicatedTo </SNT> 
<SNT> capital leaderName </SNT> <SNT> legislature </SNT> <SNT> location dedicatedTo </SNT> 
<SNT> location capital largestCity </SNT> <SNT> leaderTitle leader </SNT> 
<SNT> birthPlace birthDate </SNT> <SNT> region residence </SNT> 
<SNT> birthPlace almaMater residence </SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> <SNT> <SNT> </SNT> <SNT> <SNT> 
<SNT> nationality birthPlace </SNT> <SNT> </SNT> <SNT> deathPlace </SNT> 
<SNT> nationality birthPlace deathPlace </SNT> 
<SNT> nationality birthPlace </SNT> <SNT> deathPlace ethnicGroup </SNT> 
<SNT> birthPlace almaMater </SNT> 
<SNT> birthPlace almaMater </SNT> 
<SNT> birthPlace birthPlace </SNT> 
<SNT> nationality leaderName leaderName </SNT> 
<SNT> nationality leaderName leaderName residence </SNT> 
<SNT> nationality leaderName leaderName residence </SNT> 
<SNT> birthDate deathPlace </SNT> 
<SNT> birthPlace deathPlace birthPlace </SNT> 
<SNT> birthPlace </SNT> 
<SNT> birthPlace </SNT> 
<SNT> deathPlace leaderName </SNT> 
<SNT> birthPlace birthDate </SNT> 
<SNT> birthPlace deathPlace </SNT> <SNT> deathPlace birthPlace </SNT> 
<SNT> birthPlace deathPlace </SNT> <SNT> deathPlace birthPlace </SNT> 
<SNT> largestCity deathPlace </SNT> <SNT> region state </SNT> 
<SNT> birthPlace region </SNT> <SNT> state largestCity </SNT> <SNT> deathPlace </SNT> 
<SNT> battles ethnicGroup commander </SNT> 
<SNT> battles ethnicGroup commander </SNT> 
<SNT> nationality language </SNT> 
<SNT> manager ground </SNT> <SNT> club club </SNT> <SNT> leader </SNT> 
<SNT> manager club club </SNT> <SNT> ground leader </SNT> 
<SNT> ground league </SNT> <SNT> champions </SNT> <SNT> leader </SNT> <SNT> language </SNT> 
<SNT> location ground </SNT> <SNT> manager club club </SNT> 
<SNT> ground leader </SNT> <SNT> demonym language capital </SNT> 
<SNT> ground demonym </SNT> <SNT> capital officialLanguage leader </SNT> 
<SNT> city state has_to_its_northwest </SNT> <SNT> established academicStaffSize </SNT> 
<SNT> city director affiliation </SNT> <SNT> numberOfPostgraduateStudents campus </SNT> 
<SNT> city country </SNT> <SNT> president affiliation </SNT> <SNT> numberOfPostgraduateStudents </SNT> 
<SNT> country affiliation headquarters </SNT> <SNT> leaderTitle leaderName </SNT> 
<SNT> city country academicStaffSize established </SNT> <SNT> largestCity </SNT> 
<SNT> state country </SNT> <SNT> largestCity leaderName river </SNT> 
<SNT> established city country </SNT> <SNT> academicStaffSize </SNT> <SNT> leaderName </SNT> 
<SNT> city governmentType has_to_its_northeast </SNT> <SNT> affiliation headquarters </SNT> 
<SNT> author precededBy followedBy language </SNT> 
<SNT> city publisher affiliation </SNT> <SNT> president affiliation </SNT> 
<SNT> publisher country ethnicGroup language spokenIn </SNT> 
<SNT> publisher country ethnicGroup language spokenIn </SNT> 
<SNT> publisher country ethnicGroup language spokenIn </SNT> 
<SNT> country language spokenIn </SNT> <SNT> capital ethnicGroup </SNT> 
<SNT> country precededBy precededBy followedBy followedBy </SNT> 
<SNT> country language spokenIn </SNT> <SNT> leaderName ethnicGroup </SNT> 
<SNT> country leaderName language spokenIn </SNT> <SNT> ethnicGroup </SNT> 
<SNT> precededBy country leaderName capital </SNT> <SNT> ethnicGroup </SNT> 
<SNT> demonym ethnicGroup </SNT> <SNT> capital leaderName </SNT> <SNT> country </SNT> 
<SNT> country </SNT> <SNT> ethnicGroup leaderTitle </SNT> <SNT> language spokenIn </SNT> 
<SNT> nationality birthPlace birthDate </SNT> <SNT> occupation timeInSpace </SNT> <SNT> status </SNT> 
<SNT> birthPlace deathPlace </SNT> <SNT> was_a_crew_member_of operator awards higher </SNT> 
<SNT> birthName was_a_crew_member_of operator backup_pilot </SNT> <SNT> commander dateOfRetirement </SNT> 
<SNT> nationality birthPlace </SNT> <SNT> almaMater was_selected_by_NASA occupation </SNT> <SNT> was_a_crew_member_of </SNT> 
<SNT> birthPlace birthDate </SNT> <SNT> almaMater was_a_crew_member_of operator </SNT> <SNT> backup_pilot </SNT> 
<SNT> nationality birthPlace </SNT> <SNT> occupation was_a_crew_member_of operator </SNT> <SNT> backup_pilot </SNT> 
<SNT> birthDate birthPlace </SNT> <SNT> almaMater was_selected_by_NASA </SNT> <SNT> occupation was_a_crew_member_of </SNT> 
<SNT> senators gemstone </SNT> <SNT> deathPlace </SNT> <SNT> awards higher </SNT> 
<SNT> nationality birthPlace </SNT> <SNT> almaMater </SNT> <SNT> occupation was_a_crew_member_of </SNT> <SNT> dateOfRetirement </SNT> 
<SNT> birthPlace was_a_crew_member_of operator crewMembers </SNT> <SNT> dateOfRetirement </SNT> 
<SNT> almaMater </SNT> <SNT> was_a_crew_member_of operator backup_pilot crewMembers </SNT> 
<SNT> established location location state country </SNT> <SNT> category </SNT> 
<SNT> has_to_its_west has_to_its_north </SNT> <SNT> has_to_its_southeast has_to_its_southwest </SNT> <SNT> location category </SNT> 
<SNT> designer material dedicatedTo </SNT> <SNT> location capital leader </SNT> 
<SNT> nativeName designer material dedicatedTo </SNT> <SNT> location leaderName </SNT> 
<SNT> established category district location state country </SNT> 
<SNT> city location country established </SNT> <SNT> dean academicStaffSize </SNT> 
<SNT> campus state country </SNT> <SNT> numberOfPostgraduateStudents affiliation </SNT> 
<SNT> campus country established </SNT> <SNT> president affiliation </SNT> 
<SNT> country affiliation headquarters </SNT> <SNT> leaderTitle leaderName religion </SNT> 
<SNT> city country affiliation headquarters </SNT> <SNT> dean leaderName </SNT> 
<SNT> state country established </SNT> <SNT> largestCity leaderName river </SNT> 
<SNT> country ethnicGroup capital </SNT> <SNT> leaderName patronSaint </SNT> <SNT> anthem </SNT> 
<SNT> city country established </SNT> <SNT> numberOfStudents academicStaffSize </SNT> <SNT> leaderName </SNT> 
<SNT> nationality birthPlace birthDate </SNT> <SNT> almaMater occupation was_a_crew_member_of </SNT> <SNT> status </SNT> 
<SNT> birthDate birthPlace almaMater </SNT> <SNT> occupation was_selected_by_NASA </SNT> <SNT> deathPlace </SNT> 
<SNT> nationality birthPlace </SNT> <SNT> occupation was_a_crew_member_of operator </SNT> <SNT> commander backup_pilot </SNT> 
<SNT> alternativeNames birthDate birthPlace </SNT> <SNT> almaMater </SNT> <SNT> occupation was_selected_by_NASA was_a_crew_member_of </SNT> 
<SNT> nationality birthPlace birthDate </SNT> <SNT> almaMater occupation was_a_crew_member_of </SNT> <SNT> timeInSpace </SNT> 
<SNT> nationality birthPlace birthDate </SNT> <SNT> almaMater </SNT> <SNT> was_a_crew_member_of awards </SNT> <SNT> status </SNT> 
<SNT> birthDate birthPlace </SNT> <SNT> was_selected_by_NASA was_a_crew_member_of operator </SNT> <SNT> status </SNT> 
<SNT> nationality birthPlace </SNT> <SNT> almaMater </SNT> <SNT> was_a_crew_member_of operator backup_pilot </SNT> <SNT> status </SNT> 
<SNT> nationality birthPlace birthDate </SNT> <SNT> occupation was_a_crew_member_of </SNT> <SNT> dateOfRetirement </SNT> 
<SNT> nationality birthPlace </SNT> <SNT> was_a_crew_member_of operator crewMembers </SNT> <SNT> dateOfRetirement </SNT> 
<SNT> birthPlace birthDate </SNT> <SNT> occupation was_selected_by_NASA </SNT> <SNT> was_a_crew_member_of timeInSpace </SNT> <SNT> dateOfRetirement </SNT> 
<SNT> leaderName capital largestCity currency location material inaugurationDate </SNT> 
<SNT> leaderTitle leader location designer </SNT> <SNT> material inaugurationDate </SNT> 
<SNT> leaderTitle leaderName </SNT> <SNT> capital largestCity </SNT> <SNT> currency </SNT> <SNT> location inaugurationDate </SNT> 
<SNT> city country </SNT> <SNT> established academicStaffSize numberOfStudents </SNT> <SNT> dean </SNT> <SNT> leaderTitle </SNT> 
<SNT> city country established state </SNT> <SNT> president director motto </SNT> 
<SNT> has_to_its_northeast has_to_its_west </SNT> <SNT> state was_given_the_'Technical_Campus'_status_by location </SNT> <SNT> sportsOffered sportsGoverningBody </SNT> 
<SNT> city country </SNT> <SNT> capital leaderTitle leaderName </SNT> <SNT> ethnicGroup anthem </SNT> 
<SNT> city country established </SNT> <SNT> dean academicStaffSize leaderName </SNT> <SNT> affiliation </SNT> 
<SNT> city country affiliation headquarters </SNT> <SNT> leaderTitle leaderName </SNT> <SNT> governmentType </SNT> 
<SNT> city country </SNT> <SNT> established numberOfStudents </SNT> <SNT> dean </SNT> <SNT> affiliation </SNT> <SNT> latinName </SNT> 
